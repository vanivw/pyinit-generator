# {{ project_name }}

Generated by **PyInit**.

## Quickstart

```bash
# (optional) create & activate venv
python -m venv .venv
source .venv/bin/activate

# install deps (Poetry/Hatch/PDM based on selection)
{% if package_manager == "poetry" -%}
pip install -U pip
pip install poetry
poetry install
poetry run pytest -q
{% elif package_manager == "hatch" -%}
pip install -U pip
pip install hatch
hatch env create
hatch run test
{% else -%}
pip install -U pip
pip install pdm
pdm install
pdm run pytest -q
{% endif %}
```